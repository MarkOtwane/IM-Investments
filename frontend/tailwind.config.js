/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{html,ts}", // Scan component HTML + TS for class names
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
